meta {
  name: Create a new journal
  type: http
  seq: 1
}

post {
  url: {{BACKEND_URL}}/api/journals/
  body: json
  auth: inherit
}

body:json {
  {
    "name": "Ticos"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
